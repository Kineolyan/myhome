<% allow_empty ||= false %>

<% if transactions && (allow_empty || !transactions.empty?) %>

<table id="transactions-table" class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Date</th>
      <th>Libellé</th>
      <th>Montant</th>
      <th>Compte</th>
      <th>Paiement</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <%= render transactions %>
  </tbody>
</table>

<% else %>
<p>Aucune transaction pour le moment</p>
<% end %>
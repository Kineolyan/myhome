<%= form_for(@comptes_category, html: { class: "form", role: "form" }) do |f| %>
  <% if @comptes_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comptes_category.errors.count, "error") %> prohibited this comptes_category from being saved:</h2>

      <ul>
      <% @comptes_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nom %><br>
    <%= f.text_field :nom, class: "form-control", required: true, placeholder: "Nom de la catégorie" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
